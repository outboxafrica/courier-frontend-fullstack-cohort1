import React from 'react'
const Button = ({ text }) => {
    return (
        <div >
            <button type= 'reset' className='button'>{text}</button>             
        </div>
    )
}

export default Button
